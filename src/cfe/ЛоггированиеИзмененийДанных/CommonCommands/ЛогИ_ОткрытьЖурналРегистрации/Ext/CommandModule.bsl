&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
    
    // Получаем текущую форму
    ТекущаяФорма = ПараметрыВыполненияКоманды.Источник;
    
    // Вызываем функцию из общего модуля
    ЛогИ_ОбщийМодульВызовСервера.КомандаОткрытьЖурналРегистрации(ТекущаяФорма);
    
КонецПроцедуры